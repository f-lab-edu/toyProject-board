<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.toyproject.repository.MybatisBoardRepository">

    <resultMap id="boardMap" type="com.board.toyproject.domain.Board">
        <result property="boardId" column="board_id"/>
        <result property="memberId"     column="member_id"/>
        <result property="title" column="title"/>
        <result property="regDt"    column="reg_dt"/>
        <result property="modDt"    column="mod_dt"/>
        <result property="content"    column="reg_dt"/>
    </resultMap>
    <insert id="saveBoard" useGeneratedKeys="true" keyProperty="board_i">
        insert into board (member_id, title, reg_dt, content, mod_dt)
        values (#{member_id}, #{title}, now(), #{content}, now())
    </insert>
    <!--<select id="findBoardByBoardId" resultType="com.board.toyproject.domain.Board" resultMap="boardMap">
        SELECT
    </select>

    findBoardByBoardId
    findBoardByMemberId
    findBoardByTitle
    findAll
    deleteBoard
    updateBoard-->
</mapper>
    
    

        
        